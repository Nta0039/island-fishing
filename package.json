{
  "name": "island-fishing-multiplayer",
  "version": "1.0.0",
  "private": true,
  "description": "3D cross-platform multiplayer island fishing game (Three.js + Socket.io)",
  "main": "server.js",
  "scripts": {
    "build": "node --check server.js && node --check db.js",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
